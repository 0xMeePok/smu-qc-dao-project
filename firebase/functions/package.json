{
  "name": "qc-dao-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Server-side wallet signature verification for QC DAO",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth,storage",
    "deploy": "firebase deploy --only functions",
    "test": "SIWE_ALLOWED_HOSTS=qc-dao-demo--test-cd-wm5z6dh8.web.app firebase emulators:exec --config ../firebase.json --only functions,firestore,auth,storage --project qc-dao-demo \"node --test test/*.test.mjs\"",
    "test:attach": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "14.3.0",
    "firebase-functions": "7.3.2",
    "viem": "2.55.19"
  },
  "devDependencies": {
    "firebase-tools": "14.27.0"
  },
  "overrides": {
    "gaxios": {
      "uuid": "11.1.1"
    },
    "teeny-request": {
      "uuid": "11.1.1"
    }
  }
}
