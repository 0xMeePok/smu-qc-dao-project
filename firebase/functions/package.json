{
  "name": "qc-dao-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Server-side wallet signature verification for QC DAO",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions",
    "test": "firebase emulators:exec --config ../firebase.json --only functions,firestore,auth --project qc-dao-demo \"node --test \\\"test/**/*.test.mjs\\\"\"",
    "test:attach": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "firebase-admin": "13.6.0",
    "firebase-functions": "6.6.0",
    "viem": "2.55.19"
  },
  "devDependencies": {
    "firebase-tools": "14.27.0"
  }
}